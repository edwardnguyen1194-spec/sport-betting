# Hourly audit 2026-04-17T19 UTC

## Synthesis (3-5 sentences)

Website was briefly unreachable (client-timeout on TCP) — root cause was request saturation
following the back-to-back deploys + aggregator-sport timeouts stacking up behind a single
gunicorn worker + 8 threads. Machine restart restored dashboard (HTTP 200 in ~6s). On restart
the drawdown circuit-breaker fired correctly: bankroll $780.62 vs peak $1000 = **21.94%
drawdown → halted: true**. No new bets are being placed until Uncle manually resumes via
`POST /api/reset-halt`. Bug-scan clean (0 bugs, health_score was 100 before halt penalty).

## Agent A — Bug hunt

Executed directly (sub-agent bash denied). Force-ran `/api/hourly-run`: **0 bugs**, empty
`bug_counts`, no anomalies. All integrity gates (invalid_market, invalid_price,
invalid_confidence, invalid_stake, duplicate_game, orphan_closed_in_open,
ledger_corruption, silent_strategy) green.

## Agent B — Live health

| Metric | Value | Status |
|---|---|---|
| Bankroll | $780.62 | ⚠️ |
| Peak bankroll | $1000.00 | — |
| Drawdown | **21.94%** | 🔴 halt |
| Halted | **true** | 🔴 manual resume required |
| Open bets | — (halt engaged) | — |
| Open exposure | $550 / 70.46% of live bankroll | 🔴 over live-cap |
| Stale bets >48h | 0 | ✅ |

Halt triggered as designed (Agent-3 safeguard). Exposure 70% of live bankroll exceeds the
75% cap only because peak-based cap was set against $1000 peak — open bets already placed
before the drawdown dropped bankroll below them. No action required except the manual
resume decision (Uncle).

## Agent C — Strategy drift

Skipped this cycle — site instability made the aggregated 24h comparison unreliable.
Will reassess next :17.

## Agent D — Line integrity

Skipped — aggregator was timing out during the instability window. Quick spot-check on next
cycle.

## Agent E — Research (NBA pace, rotation slot 3)

**One actionable insight:** Rest-differential on NBA back-to-backs suppresses combined
scoring by ~6–10 points vs. fresh baseline. Books adjust imperfectly, especially when the
prior game was an OT win or crossed 2+ time zones. The UNDER is the exploit.

**Concrete code-change:** Add `rest_days` + `travel_miles` to `team_scoring.TeamScoringTracker`,
pull pace from the free `nba_api` package (`boxscoreadvancedv3` has `pace`, `possessions`),
apply totals deltas: **−4 pts** when both teams on B2B, **−2 pts** when one, **−1 pt**
when prior game was OT or > 1,500 travel miles. Flag UNDER when book total exceeds the
pace+rest-adjusted projection by 4+ pts.

Sources: heatcheckhq.io, nbastuffer.com, github.com/swar/nba_api

## Action taken

1. `flyctl machines restart 830d54f7656928` — restored site (HTTP 000 → 200).
2. **No auto-resume of halt.** Halt is a deliberate safety, Uncle should approve.

## Next-hour follow-ups

- Run Agent C + D properly after site stabilizes.
- If Uncle wants to resume: `POST /api/reset-halt` rebases peak to current bankroll and
  clears halt.
- Evaluate adding NBA B2B rest-days feature (Agent E finding).
