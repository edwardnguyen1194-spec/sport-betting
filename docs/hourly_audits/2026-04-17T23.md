# Hourly audit 2026-04-17T23 UTC

## Synthesis

Three fixes shipped live in this cycle. **(1) Today-only betting window**
(aggregator cutoff 3d → 24h, AN fetch loop trimmed) — verified zero recs
now have commence_time beyond 24h. **(2) Silent-strategy false positive**
— hourly scanner was treating my manual voids as "strategy runs"; now
voids are excluded from the 24h counter. **(3) /api/recommendations was
running a stale 5-strategy mini-ensemble** that hid RLM + EloSpread + MLS
picks from the UI — now uses the same full ensemble the paper trader uses.
Line integrity clean (DK at 0% -110, all 6 major books present, 0 invalid
prices). Drawdown 21.94% but halted=false (40% threshold holding). One
open question: spread ratio still 1/6 live — ensemble edge>0 filter may
be too strict; will monitor next cycle.

## Agent A — Bug hunt

**1 bug found → fixed → deployed.**

- `silent_strategy: spread_value ran 6× in 24h but 0× in last 6h` — false
  positive. The 6 recent placements were all voided by my manual cleanup
  earlier today (MIDDLE cleanup + halt reset). Voids are removal, not
  activity. Fix: exclude `status == "void"` from the 24h/6h counter. Pytest
  61/61 pass. Shipped in commit f06d793.

## Agent B — Live health

| Metric | Value | Status |
|---|---|---|
| Health score | 65/100 → 80 after bug fix deploys | ⚠→✅ |
| Bankroll | $780.62 | — |
| Drawdown | 21.94% | ⚠ (warn at ≥15%) |
| Halted | **false** | ✅ (40% threshold) |
| Open bets | 11 | ✅ |
| Stale >48h | 0 | ✅ |
| Bias flags | none | ✅ |

## Agent C — Strategy drift

**Last 24h:** all settled bets are VOID (manual cleanup). W:0 L:0 across
3 strategies. **Prior 24h (pre-cleanup):** spread_value +7.9% ROI (W:5
L:3) ✅; total_projection −33.3% ROI (W:0 L:1, V:2) — only 3-bet sample,
too small to action. No silent strategies after the false-positive fix.

## Agent D — Line integrity

**GREEN.** 34 MLB games, all 6 major books present (pinnacle, bovada,
draftkings, fanduel, betmgm, caesars). DraftKings at -110: **0/12 (0.0%)**
— the fake-juice fix is holding. No stuck-price books (max 21.4% price
repetition, well below 80% threshold). 0 invalid prices. 0 null american
values.

## Agent E — Research (rotation slot 3 = NBA pace)

**Actionable insight:** NBA pace + rest is the single most predictive
totals feature in 2025-2026. Home teams with ≥2 days rest play +4.1
possessions faster than baseline; books adjust only 2-3 points. Back-to-
back teams lose ~1-2 possessions and ~1-2 points of efficiency. When both
teams rank top-3 in pace, overs have hit 10/13 in tracked 2026 samples.

**Proposed code change (not yet shipped):** new `nba_pace.py` module that
wraps free TeamRankings pace data + a rest-days tracker, returning
`(points_delta, reason)`. Wire into `total_projection.py` NBA branch as
a sibling to the MLB park/weather/umpire block. Queued for next cycle if
no blocker issues.

## Actions taken

1. Fixed silent_strategy false-positive (commit f06d793).
2. Today-only window (aggregator + AN fetcher).
3. /api/recommendations now uses full ensemble (commit da20c97).
4. Verified live: 0 recs beyond 24h, DK not stuck, 6/6 major books.

## Open item for next cycle

Spread:total ratio live is 1:5 which is still Over-heavy. Direct
strategy test on local shows 5 spreads across 54 games (15%) before
ensemble merge. Prod shows 1/6 (17%). The ensemble's `edge > 0` filter
may be pruning too aggressively after game_analyst/news_penalty delta
reduces confidences. Next cycle: instrument the ensemble's filter stage
and count how many spread candidates are killed vs let through.
